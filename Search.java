package library;
import javax.swing.*;
import BreezySwing.*;
public class Search extends GBDialog{
	public Search(JFrame parent) {
		super(parent);
		setTitle("Search");
		setSize(300,100);
		setVisible(true);
		
	}

}
