package library;
import javax.swing.*;
import BreezySwing.*;
public class LoanOut extends GBDialog{
	public LoanOut(JFrame parent) {
		super(parent);
		setTitle("Loan Out");
		setSize(300,100);
		setVisible(true);
		
	}

}