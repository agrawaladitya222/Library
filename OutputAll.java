package library;
import javax.swing.*;
import BreezySwing.*;
public class OutputAll extends GBDialog{
	public OutputAll(JFrame parent) {
		super(parent);
		setTitle("Books in Library");
		setSize(300,100);
		setVisible(true);
		
	}

}
